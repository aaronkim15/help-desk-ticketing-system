{
  "name": "helpdesk-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "db:create": "createdb helpdesk || true",
    "db:schema": "psql -d helpdesk -f ../database/schema.sql",
    "db:setup": "npm run db:create && npm run db:schema"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "pg": "^8.19.0"
  }
}
